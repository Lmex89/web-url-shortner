
services:
  # URL Shortener Frontend Application
  url-shortener:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: url-shortener-app
    ports:
      - "3010:3000"
    environment:
      # Production environment variables
      - VITE_NODE_ENV=production
      - VITE_REACT_APP_API_URL=REPLACE_API_URL
      - VITE_REACT_APP_USE_MOCK_API=false
      - VITE_REACT_APP_ENABLE_DEBUG=false
      - VITE_REACT_APP_ENABLE_ANALYTICS=true
    restart: unless-stopped
    networks:
      - url-shortener-network


networks:
  url-shortener-network:
    driver: bridge

# Optional: Add volumes for logs or persistent data
volumes:
  app-logs: 
